<html>
<head>
  <meta charset="UTF-8">
  <title>MoneyTrack - Регистрация</title>
  <%= csrf_meta_tags %>
  <%= stylesheet_link_tag 'registration', media: 'all' %>
</head>
<body>
<header class="header">
  <h1>MoneyTrack</h1>
  <p>Регистрация нового аккаунта</p>
</header>

<main class="registration-container">
  <%= form_with model: @user, url: users_path, local: true, id: "registration-form" do |f| %>

    <% if @user.errors.any? %>
  <div class="error-messages-box">
    <h2>Упс:</h2>
    <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message.gsub('Email ', '') %></li>
      <% end %>
    </ul>
  </div>
<% end %>



    <div class="form-group">
      <label for="fullname">Полное имя</label>
      <%= f.text_field :full_name, id: "fullname", required: true %>
      <% if @user.errors[:full_name].any? %>
        <div class="error-message">
          <%= @user.errors[:full_name].join(", ") %>
        </div>
      <% end %>
    </div>

    <div class="form-group">
      <label for="email">Email</label>
      <%= f.email_field :email, id: "email", required: true %>
      <% if @user.errors[:email].any? %>
        <div class="error-message">
          <%= @user.errors[:email].join(", ") %>
        </div>
      <% end %>
    </div>

    <div class="form-group">
      <label for="phone">Номер телефона</label>
      <%= f.telephone_field :phone, id: "phone", required: true %>
      <% if @user.errors[:phone].any? %>
        <div class="error-message">
          <%= @user.errors[:phone].join(", ") %>
        </div>
      <% end %>
    </div>

    <div class="form-group">
      <label for="password">Пароль</label>
      <%= f.password_field :password, id: "password", required: true %>
      <div class="password-requirements">
        Пароль должен содержать минимум 8 символов, включая буквы и цифры
      </div>
      <% if @user.errors[:password].any? %>
        <div class="error-message">
          <%= @user.errors[:password].join(", ") %>
        </div>
      <% end %>
    </div>

    <div class="form-group">
      <label for="confirm-password">Подтверждение пароля</label>
      <%= f.password_field :password_confirmation, id: "confirm-password", required: true %>
      <% if @user.errors[:password_confirmation].any? %>
        <div class="error-message">
          <%= @user.errors[:password_confirmation].join(", ") %>
        </div>
      <% end %>
    </div>

    <button type="submit" class="submit-button">Зарегистрироваться</button>

    <div class="login-link">
      Уже есть аккаунт? <a href="<%= login_path %>">Войти</a>
    </div>
  <% end %>
</main>

<footer class="footer">
  <div class="footer-content">
    <div class="footer-section">
      <h3>Контактная информация</h3>
      <p>📍 Москва, ул. Финансовая, 123</p>
      <p>📞 8 (800) 123-45-67</p>
      <p>📧 support@moneytrack.ru</p>
    </div>
    <div class="footer-section">
      <h3>Режим работы</h3>
      <p>Понедельник - Пятница: 9:00 - 20:00</p>
      <p>Суббота: 10:00 - 17:00</p>
      <p>Воскресенье: выходной</p>
    </div>
    <div class="footer-section">
      <h3>Социальные сети</h3>
      <div class="social-links">
        <a href="https://vk.com/moneytrack">VK</a>
        <a href="https://t.me/moneytrack">Telegram</a>
        <a href="https://www.youtube.com/moneytrack">YouTube</a>
      </div>
    </div>
  </div>
  <div class="footer-bottom">
    <p>© 2024 MoneyTrack. Все права защищены.</p>
  </div>
</footer>

<%= javascript_include_tag 'registration', defer: true %>
</body>
</html>
